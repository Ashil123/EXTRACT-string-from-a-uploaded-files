
    // console.log('---E.target', e.target.files.length);
    // const fileList = e.target.files;
    // const reader = new FileReader();
    // reader.onload = async e => {
    //   console.log('event-- ', e.target.FileReader);
    //   const text = e.target.result;
    //   const reg = new RegExp(/(?<=:).*?(?=\')/gm);
    //   const replacedString = text.match(reg);
    //   setVivoMessages([...vivoMessages, ...replacedString]);
    // };
    // for (let i = 0; i < fileList; i++) {
    //   setTimeout(async () => {
    //     await reader.readAsText(e.target.files[1]);
    //   }, 1000);